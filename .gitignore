# Godot
.godot/
*.translation

# OS / editor
.DS_Store
.vscode/
.idea/
.obsidian/

# Claude Code per-user settings
.claude/settings.local.json

# Secrets
.env
.env.*
!.env.example

# Optional Godot editor dev tooling — installed locally via
# `npx @satelliteoflove/godot-mcp --install-addon .` when wanted.
addons/godot_mcp/

# Playwright MCP scratch (console dumps, page snapshots)
.playwright-mcp/

# Stray Obsidian vault accidentally created at repo root
/zoo-tycoon/
